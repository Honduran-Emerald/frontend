export interface Location {
    latitude: number,
    longitude: number
}

export interface User {
    userId: string,
    userName: string,
    image: string,
    level: number,
    experience: number,
    glory: number,
    questCount: number,
    trackerCount: number
}
